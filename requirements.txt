# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.*
uvicorn[standard]==0.30.*
python-multipart==0.0.*        # required for FastAPI Form() and File() uploads
pydantic==2.*

# ── PDF parsing ───────────────────────────────────────────────────────────────
pypdf==4.*

# ── ML / NLP ──────────────────────────────────────────────────────────────────
torch==2.3.*                   # CPU build is fine; ~750MB but needed by all models
transformers==4.41.*
sentence-transformers==3.*     # all-MiniLM-L6-v2 embedding model
tokenizers==0.19.*             # pulled in by transformers but pin to avoid mismatch

# ── Vector database ───────────────────────────────────────────────────────────
chromadb==0.5.*

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit==1.35.*
requests==2.32.*               # Streamlit uses this to call FastAPI
httpx==0.27.*                  # Ollama API calls from summarizer

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv==1.0.*           # loads .env file into os.environ

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.*
pytest-asyncio==0.23.*         # for any async test functions